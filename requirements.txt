# ---------- Deep Learning & Computer Vision ----------
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
ultralytics>=8.0.0

# Use headless OpenCV for server/containers. Swap to opencv-python on dev machines if needed.
opencv-python-headless>=4.8.0

# ---------- Object Tracking & Utilities ----------
supervision>=0.16.0
lap>=0.4.0
scipy>=1.10.0

# ---------- OCR ----------
easyocr>=1.7.0

# ---------- Data Processing & Visualization ----------
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
tqdm>=4.66.0
pyyaml>=6.0

# ---------- FastAPI / Websocket / Server ----------
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# ---------- Task Queue & Redis ----------
celery>=5.3.0
redis>=5.0.0

# ---------- Database (Postgres) ----------
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9

# ---------- Monitoring & Experiment Tracking ----------
prometheus-client>=0.19.0
mlflow>=2.9.0

# ---------- Configuration & Validation ----------
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# ---------- Utilities & Security ----------
pillow>=10.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ---------- Optional / Helpful Dev Tools ----------
# (Keep these in dev/requirements or include here if needed)
# black, flake8, pytest, mypy etc. are useful for CI/testing but omitted here.
